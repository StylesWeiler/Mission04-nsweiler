@{
    Layout = "_Layout";
}
@* The "_" before a file name means that it is a supporting file. One that we do not access directly but
    is called within another file. Private scope.

    This line above indicates what layout we will use
*@
